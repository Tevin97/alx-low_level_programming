#include <stdio.h>
/**
 * main - a program that prints a line with put function
 * return: 0 (success!) zero when the program is successful
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
