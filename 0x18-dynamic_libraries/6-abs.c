/**
 * _abs - computes absolute value of an integer
 * @n: the integer to compute the absolute value
 * Return: result of the computations
 */
int _abs(int n)
{
	if (n < 0)
	return (-1 * n);
	else
	return (n);
}
